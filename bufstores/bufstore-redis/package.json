{
  "name": "@jobor/bufstore-redis",
  "description": "Redis job buffer bufstore for jobor",
  "version": "0.3.1",
  "keywords": [],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=16"
  },
  "author": "TY",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "nb-tsc -b",
    "clean": "del 'jobor-bufstore-redis*.tgz' 'dist/*' '*.tsbuildinfo' 'package'",
    "pretest": "yarn run clean && yarn run build",
    "test": "yarn run pretest && yarn run jest",
    "jest": "jest"
  },
  "devDependencies": {
    "@jobor/test-suite": "workspace:^",
    "@nutol/build": "^0.2.15",
    "@nutol/eslint-config": "^0.2.10",
    "@nutol/ts-config": "^0.2.2",
    "@types/jest": "^29.5.7",
    "@types/node": "^20.8.10",
    "del-cli": "^5.1.0",
    "jest": "^29.7.0"
  },
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ]
}
